services:
  api:
    build: .
    depends_on:
      db:
        condition: service_started
      redis:
        condition: service_started
    volumes:
      - .:/usr/app/
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://postgres:root@db/postgres
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/0
    networks:
      - api
  
  db:
    image: postgres:18-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=root
      - POSTGRES_DB=postgres
    ports:
      - "5434:5432"
    networks:
      - api
  
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    networks:
      - api
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
  
  celery_worker:
    build: .
    entrypoint: ["/usr/app/celery_worker_entrypoint.sh"]
    depends_on:
      - db
      - redis
    volumes:
      - .:/usr/app/
    environment:
      - DATABASE_URL=postgresql://postgres:root@db/postgres
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/0
    networks:
      - api
    restart: unless-stopped
  
  celery_beat:
    build: .
    entrypoint: ["/usr/app/celery_beat_entrypoint.sh"]
    depends_on:
      - db
      - redis
      - celery_worker
    volumes:
      - .:/usr/app/
    environment:
      - DATABASE_URL=postgresql://postgres:root@db/postgres
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/0
    networks:
      - api
    restart: unless-stopped

volumes:
  postgres_data:

networks:
  api: 
    name: api

